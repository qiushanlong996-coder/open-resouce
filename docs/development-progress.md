# 新猿译码开发进度

本文档是跨电脑开发的持续交接记录。每完成并推送一个小功能，都必须同步更新本文件。

## 环境与人工安装事项

- 自动安装或自动配置环境失败时，必须立即在本节记录，并通知项目负责人手动处理。
- 记录内容必须包含：目标机器、软件名称、准确版本、用途、安装或验证命令、是否阻塞当前功能。
- 当前本机没有系统级 Go。开发流程临时使用工作区内 Go 1.24.6，不阻塞当前开发。
- 当前服务器没有安装 Go。Gateway 使用本地交叉编译的静态二进制部署，不阻塞当前开发。
- 服务器 Docker 镜像源连接不稳定。进入数据库与中间件 Compose 部署前需要再次验证；若仍失败，需要项目负责人协助手动配置稳定镜像源。
- 已解决：HTTPS 连接 GitHub 443 连续超时。2026-07-26 已生成并添加专用 ed25519 密钥，GitHub SSH 认证成功，仓库远程地址已切换为 SSH。
- 2026-07-31：新 macOS 开发机初始无 Go。brew 从 ghcr.io 下载极慢不可用；已由项目负责人通过阿里云镜像安装官方 Go 1.24.6 pkg（/usr/local/go），并将 GOPROXY 设为 goproxy.cn，不阻塞开发。
- 2026-07-31：本机无 pnpm，且 apps/web 的 package-lock.json 已过期；前端依赖统一使用 `npx pnpm@10 install --frozen-lockfile` 安装（与 pnpm-lock.yaml 一致），不阻塞开发。

## 当前基线

- 当前分支：`main`
- 当前阶段：一期第 1 周，后端基础骨架
- 前端：`apps/web` React Demo，可构建、可本地预览
- 后端：开始实现 `services/gateway`
- 生产服务器：`www.lovenuaa.xyz`，CentOS 7

## 2026-07-26：Gateway 健康检查

### 已完成

- 新增根 Go Module，目标版本为 Go 1.24。
- 新增 Gateway 服务入口。
- 新增 `GET /healthz` 与 `GET /readyz`。
- 两个接口均返回：

```json
{"service":"gateway","status":"ok"}
```

- 新增优雅停机处理，响应 `SIGINT` 和 `SIGTERM`。
- 新增健康检查及未知路由测试。
- 更新开发部署文档中的 Gateway 启动与验证命令。

### 验证

- 已使用 Go 1.24.6 完成 Linux/amd64 测试二进制编译。
- 已在生产服务器隔离目录运行完整测试，健康检查和未知路由测试全部通过。
- Windows 本机应用控制策略禁止执行临时编译的测试程序，因此采用交叉编译后在 Linux 服务器运行的验证方式。
- 已部署并验证 `http://127.0.0.1:18080/healthz`。

### 服务器现状与注意事项

- 服务器已有业务正在使用 `www.lovenuaa.xyz`、Nginx 和本机 `8080` 端口。
- `8080` 当前由 Java 服务监听，禁止占用或停止。
- 现有 Nginx 配置指向 `/opt/more-offer/current/frontend/dist`，禁止覆盖。
- 新猿译码使用独立目录 `/opt/open-resouce`。
- Gateway 预留宿主机端口 `18080`，未完成隔离部署前不要修改现有 Nginx。
- 服务器没有安装 Go；Docker CLI 已安装，但初次检查时守护进程未运行。
- Docker Hub 当前连接不稳定，国内镜像源也出现过 TLS 握手超时；部署脚本不能假设每次都能在线拉取构建镜像。
- 不得把服务器密码、JWT 密钥或其他敏感配置写入仓库。

### 下一步

- [x] 完成 Go 1.24 环境中的测试。
- [ ] 增加 Gateway 多阶段 Dockerfile。
- [x] 将 Gateway 隔离部署到服务器 `18080`。
- [ ] 确定独立 API 子域名或 Nginx 路由。

## 2026-07-26：Gateway systemd 隔离部署

### 已完成

- Gateway 默认监听地址调整为 `127.0.0.1:8080`。
- 新增 `HOST` 与 `PORT` 环境变量，支持按环境覆盖监听地址。
- 新增监听地址配置测试。
- 新增 `deploy/systemd/open-resouce-gateway.service`。
- 使用 Go 1.24.6 交叉编译静态 Linux/amd64 二进制。
- 部署目录：`/opt/open-resouce/current/bin/gateway`。
- systemd 服务名：`open-resouce-gateway`。
- 服务已设置为开机启动。

### 验证

- 更新后的全量 Gateway 测试在服务器运行通过。
- `systemctl is-active open-resouce-gateway` 返回 `active`。
- `curl http://127.0.0.1:18080/healthz` 返回：

```json
{"service":"gateway","status":"ok"}
```

- `ss -lntp` 确认服务只监听 `127.0.0.1:18080`。

### 运维命令

```bash
systemctl status open-resouce-gateway
journalctl -u open-resouce-gateway -n 100 --no-pager
systemctl restart open-resouce-gateway
curl http://127.0.0.1:18080/healthz
```

### 注意事项

- 当前 Gateway 仅供服务器本机访问，公网和现有域名不会转发到该端口。
- 未确定独立 API 域名之前，不修改现有 `www.lovenuaa.xyz` 的 Nginx 路由。
- 后续部署必须先通过测试，再替换二进制并重启 systemd 服务。
- 若部署失败，检查 `journalctl -u open-resouce-gateway`，不要停止占用 `8080` 的既有 Java 服务。

### 下一步

- [x] 新增统一 JSON 响应、请求 ID 和访问日志中间件。
- [ ] 增加 API 版本入口 `/api/v1`。
- [ ] 为前端准备可配置的 Gateway 基础地址。

## 2026-07-26：Gateway 请求追踪与统一错误

### 已完成

- 新增 `X-Request-ID` 中间件。
- 客户端提供合法长度的请求 ID 时原样透传；未提供或超过 128 字符时自动生成。
- 新增结构化访问日志，记录请求 ID、方法、路径、状态码和耗时。
- 未知路由统一返回 JSON 错误 `route_not_found`。
- 不支持的请求方法统一返回 JSON 错误 `method_not_allowed`，并设置 `Allow` 响应头。
- 抽取统一 JSON 响应方法，为后续 API 接口复用。

### 验证

- Go 1.24.6 Linux/amd64 编译成功。
- 更新后的 8 组测试及子测试全部通过。
- 已原子替换服务器 Gateway 二进制并重启服务。
- 带 `X-Request-ID: deploy-check-001` 请求健康检查，响应头正确透传。
- 请求 `/missing` 返回 HTTP 404 和统一 JSON 错误。
- `journalctl` 已确认访问日志包含请求 ID、方法、路径、状态码和耗时。

### 卡点与注意事项

- 非阻塞：服务器 `journalctl` 的历史日志起止时间显示顺序异常，可能经历过系统时间或时区调整。当前实时日志时间正确，暂不影响 Gateway；后续进行日志聚合或证书自动化前应检查 `timedatectl` 和 NTP。
- 客户端应保存响应中的 `X-Request-ID`，以便后续排查问题。

### 下一步

- [x] 新增 `/api/v1` 版本入口和服务信息接口。
- [ ] 增加安全响应头与 CORS 基础策略。
- [ ] 为前端准备统一 API Client。

## 2026-07-26：API v1 版本入口

### 已完成

- 新增 `GET /api/v1` 服务信息入口。
- 返回 Gateway 服务名、API 版本和运行状态。
- `/api/v1/` 下尚未实现的资源继续使用统一 JSON 404 错误。
- 新增版本入口和未知版本化路由测试。

### 验证

- Go 1.24.6 Linux/amd64 编译成功。
- 更新后的 10 组测试及子测试全部通过。
- 已部署到服务器并重启 `open-resouce-gateway`。
- `GET http://127.0.0.1:18080/api/v1` 返回：

```json
{"service":"gateway","api_version":"v1","status":"ok"}
```

- systemd 服务状态为 `active`。

### 注意事项

- `/api/v1` 当前是服务发现入口，不代表项目、用户等业务接口已经实现。
- 新增业务接口必须放在 `/api/v1/` 命名空间内，避免未来版本升级破坏客户端。

### 下一步

- [x] 增加安全响应头与 CORS 基础策略。
- [ ] 为前端准备统一 API Client 和开发代理。
- [ ] 开始项目列表只读 API 的数据契约设计。

## 2026-07-26：安全响应头与 CORS

### 已完成

- 新增 API 安全响应头中间件。
- 增加 CSP、Permissions Policy、Referrer Policy、`nosniff` 和禁止 iframe 嵌入策略。
- 新增基于 `CORS_ALLOWED_ORIGINS` 的精确来源白名单。
- 支持逗号分隔配置多个前端来源。
- 允许跨域请求携带 `Authorization`、`Content-Type` 和 `X-Request-ID`。
- 向浏览器暴露 `X-Request-ID`，预检缓存 600 秒。
- 未配置白名单时，跨域预检统一返回 `origin_not_allowed` JSON 错误。

### 验证

- Go 1.24.6 Linux/amd64 编译成功。
- 更新后的 13 组测试及子测试全部通过。
- 已部署并重启 `open-resouce-gateway`。
- `/api/v1` 响应包含全部预期安全响应头。
- 未授权来源 `https://untrusted.example` 的 OPTIONS 预检返回 HTTP 403。

### 配置与注意事项

- 本地前端联调时设置：

```text
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
```

- 生产环境必须填写准确域名，不使用 `*`。
- 服务器当前未配置允许来源，因为 Gateway 尚未通过 Nginx 暴露，不影响现有服务。
- 本功能没有新增人工安装事项。

### 下一步

- [x] 为前端准备统一 API Client 和开发代理。
- [x] 在页面展示 Gateway 连通状态。
- [ ] 开始项目列表只读 API 的数据契约设计。

## 2026-07-26：前端 API Client 与连通状态

### 已完成

- 新增 `apps/web/src/api/client.ts` 统一请求客户端。
- 新增结构化 `ApiError`，保留 HTTP 状态、错误代码和请求 ID。
- 新增 Gateway 服务信息请求。
- Vite 开发服务器新增 `/api` 代理，默认目标为 `127.0.0.1:18080`。
- 新增 `.env.example`，支持覆盖浏览器 API 地址和开发代理目标。
- 首页页脚新增 Gateway 检查中、已连接和离线三种状态。
- Gateway 不可用时页面继续使用演示数据，不阻塞浏览体验。

### 验证

- TypeScript 项目构建通过。
- Vite 8.1.5 生产构建通过，共转换 1785 个模块。
- 通过临时 SSH 隧道联调本地 Vite 与服务器 Gateway。
- 桌面页面显示 `Gateway v1 已连接`。
- 390px 移动端页面显示 `Gateway v1 已连接`。
- 桌面和移动端浏览器控制台均无警告或错误。

### 卡点与注意事项

- 非阻塞：现有 `www.lovenuaa.xyz` 正在承载另一套应用，前端不能直接覆盖部署。
- 在项目负责人确认新域名、子域名或独立路径前，前端继续本地验证，Gateway 继续仅监听服务器回环地址。
- 跨电脑开发可复制 `.env.example` 为 `.env.local`；敏感信息不得放入任何 `VITE_` 变量。
- 本功能没有新增人工安装事项。

### 下一步

- [x] 设计并实现项目列表只读 API 数据契约。
- [ ] 将首页演示项目迁移为“API 数据优先、演示数据兜底”。
- [x] 增加项目列表分页、筛选和排序参数。

## 2026-07-26：项目列表只读 API

### 已完成

- 新增 `GET /api/v1/projects`。
- 定义项目摘要、指标和分页响应数据契约。
- 支持 `page` 和 `page_size`，单页最多 50 条。
- 支持 `q` 关键词搜索，覆盖名称、简介、分类、标签和技术栈。
- 支持 `category` 精确分类筛选。
- 支持按 `updated`、`downloads`、`stars` 降序排列。
- 非法分页和排序参数统一返回 `invalid_query` JSON 错误。
- 响应携带请求 ID，便于跨服务排查。

### 验证

- Go 1.24.6 Linux/amd64 编译成功。
- 首次测试发现旧“未知路由”用例与新接口冲突，已将用例修正为 `/api/v1/unknown`。
- 修正后全量测试及子测试全部通过。
- 已部署并重启 `open-resouce-gateway`。
- 请求 `q=RAG` 仅返回 `paperclip` 项目，分页总数为 1。
- systemd 服务状态为 `active`。

### 卡点与注意事项

- 当前项目列表使用 Gateway 内置种子数据，用于先冻结 API 契约，不是最终数据源。
- 接入 MySQL 后必须保持字段与分页结构兼容，避免破坏前端。
- 搜索当前是内存包含匹配；Elasticsearch 接入后再扩展高亮和相关性排序。
- 本功能没有新增人工安装事项。

### 下一步

- [x] 将首页迁移为 API 数据优先、演示数据兜底。
- [x] 将页面搜索和分类筛选传递给后端。
- [x] 增加加载、失败和空结果状态。

## 2026-07-26：首页项目目录 API 化

### 已完成

- 扩展前端 API Client，新增项目摘要和分页响应类型。
- 首页启动后从 `GET /api/v1/projects` 加载项目目录。
- 页面搜索使用 250ms 防抖，并将 `q` 参数传给后端。
- 分类切换将 `category` 参数传给后端。
- API 项目摘要映射到现有项目卡片展示模型。
- 新增加载提示和离线提示。
- API 不可用时自动回退到演示数据，页面仍可浏览。
- 修复 API 返回新对象后项目卡片编号失效的问题。

### 验证

- TypeScript 和 Vite 生产构建通过。
- 在线初始状态加载 4 张 API 项目卡片。
- 搜索 `RAG` 后仅显示 `Paperclip RAG`。
- 清空搜索后恢复 4 张项目卡片。
- 390px 移动端初始加载 4 张 API 项目卡片。
- 移动端选择 `RAG Agent` 后仅显示对应项目。
- 临时 SSH 隧道退出时，页面显示离线提示并继续展示演示数据。
- 在线与离线验证过程中浏览器控制台均无警告或错误。

### 卡点与注意事项

- 项目详情仍使用本地扩展字段；当前 API 只提供列表摘要。
- API 数据的更新时间当前显示为日期，后续统一实现相对时间格式。
- 页面搜索目前同时保留本地过滤作为保护，后端响应是最终目录来源。
- 本功能没有新增人工安装事项。

### 下一步

- [x] 实现项目详情只读 API。
- [ ] 将详情页迁移为 API 数据优先。
- [ ] 起草 OpenAPI 3.1 契约文件。

## 2026-07-26：项目详情只读 API

### 已完成

- 新增 `GET /api/v1/projects/{slug}`。
- 详情响应包含列表摘要的全部字段。
- 新增详细介绍、项目亮点、适用场景、仓库地址和当前版本。
- 空 slug、多层路径和不存在的 slug 返回 `project_not_found`。
- 非 GET 请求返回统一 `method_not_allowed`。

### 验证

- Go 1.24.6 Linux/amd64 编译成功。
- 项目详情存在、缺失路径和错误方法测试全部通过。
- Gateway 全量测试及子测试全部通过。
- 已部署并重启 `open-resouce-gateway`。
- Atlas 详情响应包含版本 `0.8.0`、3 条亮点和 3 个适用场景。
- systemd 服务状态为 `active`。

### 卡点与注意事项

- 详情数据当前仍是种子数据；接入数据库后保持响应契约兼容。
- 仓库地址当前仅用于展示，尚未实现外链安全检查或 GitHub 同步。
- 文档目录、下载资源和评论不属于本次详情响应，将由独立接口提供。
- 本功能没有新增人工安装事项。

### 下一步

- [x] 将前端详情页迁移为 API 数据优先。
- [x] 展示亮点、适用场景和当前版本。
- [ ] 起草 OpenAPI 3.1 契约文件。

## 2026-07-26：前端项目详情 API 化

### 已完成

- 扩展前端 API Client，新增项目详情类型和请求方法。
- 点击项目卡片后立即显示列表摘要，同时异步加载详情。
- 详情加载成功后更新详细介绍、仓库、当前版本、亮点和适用场景。
- 详情 API 失败时保留缓存内容并显示明确提示。
- 当前版本指标不再硬编码。
- 项目概览移除 Atlas 专属硬编码，按 API 数据渲染亮点和适用场景。

### 验证

- TypeScript 和 Vite 生产构建通过。
- 从 API 项目目录成功打开 Atlas 详情。
- 页面显示仓库完整 URL 和版本 `v0.8.0`。
- 项目概览显示 `可观察的多 Agent 运行时` 亮点。
- 项目概览显示 `复杂研究任务` 适用场景。
- 390px 移动端正确显示项目标题和当前版本。
- 浏览器控制台无警告或错误。

### 卡点与注意事项

- 非阻塞：文档阅读和代码预览仍使用 Atlas 示例内容，打开其他项目时内容不会随项目变化。
- 该问题需要文档目录、文档正文和代码文件 API，作为后续独立功能处理。
- 详情请求当前未缓存；后续引入 React Query 时增加缓存、重试和请求去重。
- 本功能没有新增人工安装事项。

### 下一步

- [x] 起草 OpenAPI 3.1 契约文件。
- [x] 增加契约结构校验。
- [ ] 开始文档目录与正文只读 API。

## 2026-07-26：OpenAPI 3.1 初稿

### 已完成

- 新增 `api/openapi/openapi.json`。
- 覆盖健康检查、就绪检查、API 版本入口、项目列表和项目详情。
- 定义查询参数、分页、项目摘要、项目详情和统一错误 schema。
- 新增零依赖 `scripts/validate-openapi.mjs`。
- 校验脚本检查 OpenAPI 版本、必需路径、GET 200 响应和内部引用。
- README 新增契约和进度文档入口。

### 验证

- OpenAPI JSON 解析成功。
- 校验结果：5 个路径、10 个 schema，全部引用有效。
- 已读取服务器项目列表响应，确认 `data`、`pagination` 和 `request_id`。
- 已读取服务器 Atlas 详情响应，确认详细介绍、亮点、场景、仓库和版本字段。
- 线上字段一致性检查通过。

### 卡点与注意事项

- 当前校验脚本负责仓库内结构和引用检查，不替代完整的 OpenAPI JSON Schema 验证器。
- 后续 CI 环境建立后，可增加 Redocly 或 Spectral；当前不为此引入新依赖。
- 每次新增或修改 API 路径、参数、字段或错误码，必须同步更新契约。
- 本功能没有新增人工安装事项。

### 下一步

- [x] 实现文档目录与正文只读 API。
- [x] 为文档内容定义稳定的块 ID 和 Markdown 字段。
- [ ] 将在线文档页面从静态示例迁移到 API。

## 2026-07-26：文档目录与正文只读 API

### 已完成

- 新增 `GET /api/v1/projects/{slug}/documents`。
- 新增 `GET /api/v1/projects/{slug}/documents/{documentSlug}`。
- 文档目录节点包含稳定 ID、slug、标题、顺序和子节点。
- 文档正文包含 Markdown、版本、更新时间和标题大纲。
- 文档正文包含稳定内容块 ID、块类型和纯文本，用于后续选区评论锚定。
- 为 4 个种子项目提供快速开始文档，Atlas 提供完整示例正文。
- 同步更新 OpenAPI 路径与文档 schema。

### 验证

- Go 1.24.6 Linux/amd64 编译成功。
- 文档目录、正文、项目不存在和文档不存在测试全部通过。
- Gateway 全量测试及子测试全部通过。
- OpenAPI 校验通过：7 个路径、16 个 schema。
- 已部署并重启 `open-resouce-gateway`。
- Atlas 文档目录返回 `quick-start`。
- Atlas 正文返回 4 项大纲和 4 个稳定内容块。
- systemd 服务状态为 `active`。

### 卡点与注意事项

- 当前每个项目只有一篇种子“快速开始”，后续由 MongoDB 文档树替换。
- 稳定块 ID 一旦被评论引用，不应因普通正文编辑而变化。
- Markdown 在进入前端渲染前仍需执行 HTML/XSS 安全策略。
- 本功能没有新增人工安装事项。

### 下一步

1. 将在线文档页面从静态示例迁移到 API。
2. 使用 `react-markdown`、GFM 和代码高亮渲染正文。
3. 根据 API 大纲生成文档侧栏导航。

## 2026-07-26：在线文档页面 API 化

### 已完成

- 扩展前端 API Client，新增文档目录、正文、大纲和稳定内容块类型。
- 打开项目时并行加载项目详情、文档目录和快速开始正文。
- 增加项目切换请求防串扰保护，旧项目响应不会覆盖当前项目。
- 文档侧栏目录改为使用 API 文档树，版本号改为使用正文响应版本。
- 使用 `react-markdown`、`remark-gfm` 和 `rehype-highlight` 渲染 Markdown 正文。
- 为 Markdown 标题绑定 API 大纲中的稳定 ID，并根据大纲生成页内导航。
- 补充 Markdown 表格、列表、引用、行内代码、代码块及高亮样式。
- 文档 API 加载中或失败时展示状态提示，并继续保留静态缓存内容兜底。

### 验证

- TypeScript 项目构建通过。
- Vite 8.1.5 生产构建通过，共转换 2239 个模块。
- 使用与线上 OpenAPI 契约一致的本地临时 API 完成页面联调。
- 桌面端成功加载 Atlas 文档目录、版本 `0.8.2`、Markdown 正文和 4 项大纲。
- 大纲导航能够定位到稳定标题 ID，例如 `collaboration`。
- 代码块由 highlight.js 完成语法高亮。
- 390px 移动端正文正常显示、侧栏按断点隐藏且无横向溢出。
- 桌面端和移动端浏览器控制台均无警告或错误。

### 卡点与注意事项

- 非阻塞：当前终端安全策略阻止向临时 SSH 隧道进程注入服务器密码，因此本次前端交互使用同契约本地临时 API 验证；真实 Gateway 文档接口已在上一小功能完成服务器全量测试、部署和在线响应验证。
- 非阻塞：Windows 应用控制策略阻止 `oxlint.win32-x64-msvc.node` 原生模块运行，导致 oxlint 无法启动。重新安装依赖不能解除系统策略；若要求本机执行 oxlint，需要项目负责人在 Windows 应用控制中允许该原生模块，验证命令为 `cd apps/web && pnpm lint`。TypeScript 和生产构建不受影响。
- 非阻塞：生产包主 JavaScript 当前约 559 kB，Vite 提示超过 500 kB。后续可在路由或文档阅读模块引入动态导入，不影响当前功能。
- 本功能没有新增必须手工安装的软件。

### 下一步

1. 实现文档切换能力，支持同一项目下多篇文档。
2. 将文档评论与稳定内容块 ID 关联。
3. 为 Markdown 下载和标题链接按钮接入真实行为。

## 2026-07-26：文档树正文切换

### 已完成

- 文档树节点增加点击加载能力，可按节点 slug 请求对应正文。
- 切换正文时展示加载状态，当前正文会保留到新响应成功返回，避免页面闪空。
- 增加独立的文档请求序号，快速切换项目或文档时，过期响应不会覆盖当前正文。
- 返回首页时主动使在途文档请求失效。
- 当前文档节点根据正文 slug 显示选中状态。

### 验证

- TypeScript 和 Vite 生产构建通过，共转换 2239 个模块。
- 使用包含“快速开始”和“架构设计”两篇文档的契约测试 API 验证。
- 点击“架构设计”后，标题、正文、页内大纲和目录选中状态均切换到第二篇文档。
- 切换后浏览器控制台无警告或错误。

### 卡点与注意事项

- 线上种子数据当前每个项目仍只有一篇“快速开始”，切换能力需要在后续接入多文档数据后才会在线上出现多个可点击节点。
- 文档请求目前没有客户端缓存；重复切换会重新请求 API，后续引入 React Query 时统一处理。
- 本功能没有新增人工安装事项。

### 下一步

1. 为 Markdown 下载和标题链接按钮接入真实行为。
2. 将文档评论与稳定内容块 ID 关联。
3. 设计评论只读与写入 API。

## 2026-07-26：文档链接与 Markdown 下载

### 已完成

- “链接”按钮根据当前文档第一项稳定大纲 ID 生成带 hash 的完整地址并写入剪贴板。
- 剪贴板权限被浏览器拒绝时展示可操作的失败提示，避免未处理 Promise 错误。
- “下载”按钮根据当前 API 正文生成 UTF-8 Markdown 数据链接。
- 下载文件名使用 `{projectSlug}-{documentSlug}.md`，避免不同项目或文档重名。
- 文档未加载完成前禁用两个操作；加载成功后才暴露下载链接。

### 验证

- TypeScript 和 Vite 生产构建通过，共转换 2239 个模块。
- 点击链接按钮后显示“文档链接已复制”成功提示。
- 下载链接 MIME 类型为 `text/markdown;charset=utf-8`。
- Atlas 快速开始文档下载文件名为 `atlas-agent-quick-start.md`。
- 操作验证期间浏览器控制台无警告或错误。

### 卡点与注意事项

- 浏览器自动化会话无法从外层读取页面 Clipboard API 写入的内容，因此本次通过成功提示和生成逻辑验证复制动作；普通浏览器 localhost 安全上下文已允许写入。
- Markdown 当前通过数据链接下载，适合现阶段小型文档；接入大型文档或附件后应改为后端流式下载接口。
- 本功能没有新增人工安装事项。

### 下一步

1. 将文档评论与稳定内容块 ID 关联。
2. 设计评论只读与写入 API。
3. 增加评论加载、发布和解决状态。

## 2026-07-26：文档评论读写 API

### 已完成

- 新增 `GET /api/v1/projects/{slug}/documents/{documentSlug}/comments`。
- 新增 `POST /api/v1/projects/{slug}/documents/{documentSlug}/comments`。
- 新增 `PATCH /api/v1/projects/{slug}/documents/{documentSlug}/comments/{commentID}`。
- 评论数据包含文档 ID、稳定块 ID、作者、引用、正文、状态和时间字段。
- 发布评论时校验稳定 block ID 必须属于当前文档，防止评论锚点漂移到其他正文。
- 增加作者、引用和正文长度限制，并拒绝未知 JSON 字段和多段 JSON。
- 解决评论接口为幂等操作，仅允许将状态更新为 `resolved`。
- 内存评论仓库增加读写锁，保证并发请求安全。
- CORS 预检允许方法扩展为 `GET, POST, PATCH, OPTIONS`。
- OpenAPI 同步增加评论路径、请求和响应 schema。

### 验证

- Go 全量测试通过。
- 评论列表、发布、稳定块校验、解决状态、错误方法、错误正文和评论不存在用例通过。
- Linux/amd64 测试二进制交叉编译成功。
- OpenAPI 校验通过：9 个路径、21 个 schema。

### 卡点与注意事项

- 阻塞部署但不阻塞开发：当前终端安全策略禁止将服务器密码注入自动部署进程，服务器又尚未接受本机公钥，因此本次 Gateway 代码暂未部署。
- 已生成专用服务器部署公钥 `id_ed25519_lovenuaa_deploy`，等待项目负责人追加到服务器 `/root/.ssh/authorized_keys`；完成后立即补做服务器全量测试、原子替换、systemd 重启和在线接口验证。
- 当前评论仓库是进程内存数据，服务重启后新增评论会丢失；仅用于冻结 API 契约，接入 MongoDB 后保持字段兼容。
- 写接口尚未接入身份认证，当前作者来自请求正文；生产开放前必须接入登录身份并忽略客户端伪造作者。
- 本功能没有新增必须手工安装的软件。

### 下一步

1. 前端加载评论 API，并按稳定 block ID 展示。
2. 前端发布评论和解决评论改为调用 API。
3. 服务器公钥配置完成后部署并补记线上验证结果。

### 服务器部署补记

- 项目负责人已将专用部署公钥加入服务器，SSH 公钥认证验证成功。
- Linux/amd64 全量测试二进制已上传服务器运行，全部测试通过。
- 新 Gateway 二进制已通过 `gateway.next` 原子替换，并保留上一版本 `gateway.previous` 用于快速回退。
- `open-resouce-gateway` 已重启，systemd 状态为 `active`。
- 在线健康检查返回 `{"service":"gateway","status":"ok"}`。
- 在线评论列表返回种子评论及稳定锚点 `block-atlas-collaboration`。
- 在线发布请求使用不存在的稳定块时返回 HTTP 422 和 `block_not_found`。
- 服务器部署阻塞已解除；后续部署统一使用 `id_ed25519_lovenuaa_deploy` 公钥认证。

## 2026-07-26：前端评论 API 迁移

### 已完成

- 扩展前端 API Client，增加评论列表、发布和解决请求及完整类型。
- 文档正文加载后自动请求当前文档评论，切换文档时自动取消旧请求。
- API 评论映射到现有评论卡片，保留打开与已解决状态。
- 用户选中正文时，根据选区文字匹配当前文档稳定内容块并保存 block ID。
- 未选区直接添加评论时，默认锚定当前文档第一个稳定内容块。
- 发布评论改为调用线上 API，成功后把服务端返回对象加入列表。
- “解决”操作改为调用线上 PATCH API，并使用服务端状态更新评论卡片。
- 评论请求失败时展示明确提示，不再伪装成本地发布成功。

### 验证

- TypeScript 和 Vite 生产构建通过，共转换 2239 个模块。
- 通过 SSH 公钥隧道连接已部署 Gateway 完成真实线上联调。
- Atlas 文档成功加载线上种子评论，作者“林默”和稳定引用均正确显示。
- 成功发布联调评论，列表由 1 条更新为 2 条并显示同步成功提示。
- 成功将新评论标记为已解决，卡片状态和“已解决”标签同步更新。
- 评论列表、发布和解决验证期间浏览器控制台无警告或错误。

### 卡点与注意事项

- 本次联调产生一条已解决的临时评论；当前数据在 Gateway 内存中，服务下次重启后自动消失。
- 选区到稳定块的匹配当前基于纯文本包含关系；富文本跨块选区需要后续使用 DOM `data-block-id` 精确定位。
- 评论时间当前显示本地短日期时间，后续统一为相对时间并增加时区提示。
- 本功能没有新增人工安装事项。

### 下一步

1. 为 Markdown 渲染结果注入稳定 `data-block-id`，精确支持选区评论。
2. 增加评论加载和提交中的按钮状态，防止重复发布。
3. 设计登录身份与评论作者的服务端绑定。

## 2026-07-26：稳定内容块 DOM 锚点

### 已完成

- Markdown 渲染增加 ReactNode 纯文本提取，兼容标题和正文中的嵌套节点。
- 段落按 API 块类型和纯文本匹配，渲染稳定 `data-block-id`。
- 代码内容按 API code 块匹配并渲染稳定 `data-block-id`。
- 选区评论优先从选区起点最近的 `data-block-id` 获取锚点。
- 无 DOM 锚点时继续保留纯文本匹配兜底，兼容静态缓存正文。

### 验证

- TypeScript 和 Vite 生产构建通过，共转换 2239 个模块。
- 使用 SSH 公钥隧道连接已部署 Gateway 完成真实正文验证。
- Atlas 正文渲染出 4 个稳定 DOM 锚点：
  - `block-atlas-intro`
  - `block-atlas-task`
  - `block-atlas-collaboration`
  - `block-atlas-install`
- 三个段落和一个代码块的标签、块 ID 与 API 数据完全一致。
- 浏览器控制台无警告或错误。

### 卡点与注意事项

- 跨多个稳定块的选区当前锚定到选区起点所在块；后续评论模型若需要跨块范围，应增加结束 block ID 和字符偏移。
- API 块纯文本必须与 Markdown 渲染文本保持一致，否则会走文本兜底或无法注入锚点。
- 本功能没有新增人工安装事项。

### 下一步

1. 增加评论加载和提交中的按钮状态，防止重复发布。
2. 设计登录身份与评论作者的服务端绑定。
3. 将评论和文档内存仓库迁移到 MongoDB。

## 2026-07-26：评论异步交互状态

### 已完成

- 增加评论列表 `checking`、`online`、`offline` 三态。
- 评论加载期间禁用新增评论按钮并显示“加载评论中…”。
- 评论同步区域根据状态显示同步中、实时同步或离线提示。
- 发布请求期间禁用输入框、取消按钮和发布按钮，按钮显示“发布中…”。
- 空评论正文时发布按钮保持禁用。
- 解决请求按评论 ID 独立锁定，避免重复提交且不阻塞其他评论。
- 解决请求期间按钮显示“解决中…”并临时禁用回复操作。

### 验证

- TypeScript 和 Vite 生产构建通过，共转换 2239 个模块。
- 通过 SSH 公钥隧道连接线上评论 API 验证。
- 评论加载完成后同步状态显示“评论实时同步中”。
- 空正文时发布按钮不可用，填写正文后立即变为可用。
- 状态验证未发布新评论，服务器没有新增测试数据。
- 浏览器控制台无警告或错误。

### 卡点与注意事项

- 当前状态使用组件本地 state；后续引入 React Query 后应迁移为 mutation 状态并统一失效评论查询缓存。
- 评论解决失败时保持原状态并解除按钮锁定，用户可以重试。
- 本功能没有新增人工安装事项。

### 下一步

1. 设计登录身份与评论作者的服务端绑定。
2. 将评论和文档内存仓库迁移到 MongoDB。
3. 为评论 API 增加频率限制与审计日志。

## 2026-07-27：文档仓库接口抽象

### 已完成

- 定义文档仓库接口，提供项目文档列表和单篇正文读取能力。
- 将种子数据封装为 `seedDocumentRepository`，Handler 不再直接访问全局 map。
- 文档列表排序增加 slug 次级排序，保证相同 order 时结果稳定。
- 文档列表、正文和评论锚点查找统一通过仓库接口。
- 保持项目不存在与文档不存在的错误语义不变，为 MongoDB 实现预留直接替换点。

### 验证

- Go 全量测试通过。
- 新增仓库列表、正文读取、项目不存在和文档不存在单元测试。
- Linux/amd64 全量测试二进制在服务器运行通过。
- Gateway 已原子替换并重启，systemd 状态为 `active`。
- 在线 Atlas 文档目录响应结构保持兼容。

### 卡点与注意事项

- 阻塞 MongoDB 部署：服务器 Docker 23.0.1 正常运行，但 `docker manifest inspect mongo:8.0` 在 20 秒内超时，现有 5 个 registry mirror 均未解决。
- 已通知项目负责人手工配置服务器可访问的 Docker Hub 镜像源；目标镜像为 `mongo:8.0`，验证命令为 `docker pull mongo:8.0`。
- 镜像源修复前继续使用种子仓库，MongoDB 实现和容器部署暂不启用。
- 本功能没有新增本地人工安装事项。

### 下一步

1. 镜像源恢复后部署 MongoDB 8.0。
2. 实现 MongoDB 文档仓库并增加契约测试。
3. 为文档仓库增加启动探针和连接超时配置。

## 2026-07-27：Gateway 多阶段 Dockerfile

### 已完成

- 新增 `services/gateway/Dockerfile`。
- 构建阶段固定 Go 1.24 Alpine，并使用 `CGO_ENABLED=0` 生成静态 Linux/amd64 二进制。
- 构建启用 `-trimpath` 和精简链接参数。
- 运行阶段使用 `scratch`，不携带包管理器、Shell 或无关运行时。
- 容器使用非 root 用户 `65532:65532`，默认暴露 8080。
- 新增根目录 `.dockerignore`，排除前端依赖、构建产物、文档和 Codex 临时文件。
- 开发部署文档增加镜像构建与本地运行命令。

### 验证

- Docker 23.0.1 成功解析 Dockerfile 和 `.dockerignore`。
- 构建上下文压缩前传输量约 51 kB，未包含前端依赖和本地临时运行时。
- 成功解析并固定 `golang:1.24-alpine` 镜像摘要。
- 构建已进入基础镜像层下载阶段，Dockerfile 语法和 COPY 路径有效。

### 卡点与注意事项

- 非代码阻塞：服务器镜像层下载速度极慢，79 MB Go 基础层在 180 秒内仅下载约 2 MB，因此完整镜像构建未完成。
- 该现象与 MongoDB manifest 超时属于同一镜像源问题，仍需项目负责人配置可用的高速镜像源。
- 当前生产部署继续使用已验证的本地交叉编译二进制 + systemd，不受 Docker 构建速度影响。
- 本功能没有新增本地人工安装事项。

### 下一步

1. 镜像源恢复后完成 Gateway 镜像构建并验证非 root 运行。
2. 增加容器健康检查和 Compose 服务定义。
3. 部署 MongoDB 8.0 并实现文档仓库。

## 2026-07-27：Gateway 容器健康检查模式

### 已完成

- Gateway 增加 `healthcheck` 命令模式，由二进制自身请求本机 `/healthz`。
- 健康检查固定访问容器回环地址，并跟随 `PORT` 环境变量。
- 检查请求增加 3 秒超时，非 200 响应或连接失败时返回非零退出码。
- Dockerfile 默认设置 `HOST=0.0.0.0`，确保发布端口能够从容器外访问。
- scratch 镜像增加 Docker `HEALTHCHECK`，无需额外安装 curl 或 wget。
- 健康检查使用与服务相同的非 root 二进制执行。

### 验证

- Go 全量测试通过。
- 新增健康、非健康状态和自定义端口测试。
- Linux/amd64 全量测试二进制在服务器运行通过。
- Gateway 已原子替换并重启，systemd 状态为 `active`。
- 服务器执行 `PORT=18080 gateway healthcheck` 返回退出码 0。

### 卡点与注意事项

- Docker 镜像仍因基础层下载过慢未完成最终构建；健康检查命令已在同一 Linux 二进制上验证。
- systemd 当前继续使用外部 curl/服务状态检查；二进制健康检查主要用于 scratch 容器。
- 本功能没有新增人工安装事项。

### 下一步

1. 镜像源恢复后完成容器构建和 Docker 健康状态验证。
2. 增加 MongoDB Compose 服务和持久化卷。
3. 实现 MongoDB 文档仓库。

## 2026-07-27：MongoDB Compose 准备与服务器磁盘阻塞

### 已完成

- 新增 `deploy/compose/infrastructure.yml`，定义 MongoDB 8.0 服务。
- MongoDB 仅绑定 `127.0.0.1`，不会直接暴露到公网。
- 增加命名数据卷、独立后端网络、自动重启和 `mongosh` 健康检查。
- root 用户名和密码使用必填环境变量，Compose 文件不包含生产密码。
- 新增 `.env.example` 并明确忽略真实 `deploy/compose/.env`。
- 开发部署文档增加配置、校验和启动命令。

### 验证

- 已人工检查 Compose 层级、变量表达式、回环端口、命名卷和健康检查命令。
- 服务器 Docker Compose 版本为 v2.16.0。
- 未启动 MongoDB，未创建生产数据卷。

### 严重卡点与注意事项

- 阻塞所有 Docker 和数据库部署：MongoDB 镜像层下载完成后，Docker 解压返回 `structure needs cleaning`。
- 内核日志确认根卷 `/dev/mapper/centos-root` 的 XFS 出现 `XFS_WANT_CORRUPTED_GOTO` 元数据错误，不是普通镜像缓存问题。
- 已停止服务器 Docker 拉取、构建和其他非必要写盘操作；Gateway 当前仍正常运行。
- 项目负责人需要先备份重要数据，并通过控制台/KVM 进入 rescue 模式，在根卷未挂载时执行：
  1. `xfs_repair -n /dev/mapper/centos-root`
  2. 根据检查结果执行 `xfs_repair /dev/mapper/centos-root`
  3. `xfs_repair -L` 仅可在普通修复因日志损坏无法继续时作为最后手段。
- 内核同时记录 CPU 温度超过阈值并发生降频，维护窗口需要检查散热。
- 文件系统修复并重启验证前，不得执行 MongoDB Compose、Docker pull/build 或大量服务器写盘。

### 下一步

1. 等待项目负责人完成 XFS 离线修复并确认服务器重启正常。
2. 修复后复查 `dmesg`、Docker overlay2 和磁盘健康。
3. 重新拉取 MongoDB 镜像，执行 `docker compose config` 后再启动服务。

## 2026-07-27：评论仓库接口抽象

### 已完成

- 定义评论仓库接口，覆盖列表、创建和幂等解决操作。
- 将进程内存存储封装为并发安全的 `memoryCommentRepository`。
- 评论 Handler 不再直接访问内存 map，可在后续替换为 MySQL 实现。
- 空评论列表始终返回非 nil 空数组，保持 JSON 契约稳定。
- 增加评论仓库种子读取、创建、解决、不存在和空列表单元测试。

### 验证

- `go vet ./services/gateway` 通过。
- Linux/amd64 测试二进制编译成功。
- Windows 应用控制策略阻止运行临时 Go 测试可执行文件。
- 遵循 XFS 故障保护，本次没有向服务器上传测试二进制，也没有部署 Gateway。

### 卡点与注意事项

- 本小功能尚欠 Linux 全量测试运行和线上回归；必须在服务器 XFS 离线修复完成后优先补做。
- 当前生产 Gateway 仍是上一已验证版本，不包含本次评论仓库内部重构。
- 评论最终属于 MySQL 强事务数据，不应存入 MongoDB；仓库接口为 MySQL 实现预留。
- 本功能没有新增人工安装事项。

### 下一步

1. XFS 修复后运行 Linux 全量测试并部署本次重构。
2. 为评论仓库定义 MySQL schema 和迁移。
3. 为仓库错误增加统一映射与可观测指标。

## 2026-07-27：评论 MySQL 初始迁移

### 已完成

- 新增 `deploy/migrations/mysql/000001_create_document_comments.up.sql` 和对应回滚迁移。
- 评论表覆盖当前接口的文档、锚点、作者、摘录、正文、状态与时间字段。
- 预留 `parent_id` 支持后续评论回复，并为文档列表、状态筛选、回复和作者查询增加索引。
- 增加父评论自引用外键和软删除时间字段；状态与解决时间一致性由仓库事务保证。
- 文档与用户外键暂缓到对应主表落地后添加，避免迁移依赖尚未确定的表结构。
- 新增迁移目录说明和不暴露密码的本地验证命令。

### 验证

- 已人工检查 MySQL 8.0 的字段、约束、索引和正反向迁移顺序。
- 未连接生产 MySQL，未写入任何服务器数据。

### 卡点与注意事项

- 当前 Gateway 仍使用内存评论仓库；迁移尚未接入 MySQL 驱动和连接池。
- 生产执行迁移前必须先确认目标数据库名称、备份策略和 XFS 故障已修复。
- `document_id` 与 `author_id` 外键将在文档和用户主表 schema 稳定后补充。

### 下一步

1. 引入 MySQL 驱动与数据库连接生命周期管理。
2. 实现 `mysqlCommentRepository` 并补充仓库集成测试。
3. 为仓库错误增加统一映射与可观测指标。

## 2026-07-27：Gateway 接入 MySQL 评论仓库

### 已完成

- 引入 `go-sql-driver/mysql`，增加 MySQL 连接池初始化、启动探活和关闭生命周期管理。
- `DATABASE_URL` 未设置时保留内存仓库；设置后强制使用 MySQL，连接失败时拒绝启动，禁止静默降级。
- 实现 MySQL 评论列表、创建和事务内幂等解决操作。
- 评论仓库接口增加请求上下文和错误返回，Handler 将内部错误统一映射为 `repository_error`。
- 数据库时间统一按 UTC 读写，连接池限制最大连接数、空闲连接数和连接生命周期。
- systemd 服务增加受限环境文件入口，并提供不包含真实密码的配置示例。
- 增加 `DATABASE_URL` 解析和非法配置测试。
- `/healthz` 继续表示进程存活；`/readyz` 在 MySQL 探活失败时返回 503。

### 验证

- MySQL 驱动已固定到 `v1.9.3`。
- 目标服务器 MySQL 版本确认为 `5.7.39`，迁移排序规则已调整为兼容 MySQL 5.7/8.0 的 `utf8mb4_unicode_ci`。
- 本地尚未连接真实 MySQL；数据库集成测试需在测试库准备完成后执行。

### 下一步

1. 获取测试数据库账号并执行初始迁移。
2. 对 MySQL 评论仓库运行创建、列表、解决和幂等解决集成测试。
3. 增加数据库探活延迟和失败计数指标。

## 2026-07-27：MySQL 建库、真实集成测试与新服务器部署

### 已完成

- 在 `www.lovenuaa.xyz:3306` 创建 `open_resouce` 数据库并执行评论初始迁移。
- 创建独立应用账号，只授予 `open_resouce.*` 的 `SELECT`、`INSERT`、`UPDATE`、`DELETE` 权限。
- 根据真实连接来源将应用账号限制为 `103.236.98.166`，不再接受任意来源登录。
- 应用连接串保存在服务器 root 专用配置与应用服务器 `/etc/open-resouce/gateway.env`，权限为 `0600`，未写入仓库。
- MySQL 评论仓库真实集成测试通过，覆盖创建、列表、解决、幂等解决与测试数据清理。
- 在 `www.openresource.cn`（`103.236.98.166`）安装 Nginx，部署前端静态构建和 Gateway systemd 服务。
- Gateway 仅监听 `127.0.0.1:18080`，Nginx 通过同源 `/api/` 转发。
- 部署后端到端回归通过：经 Nginx 创建评论、读取列表、解决评论，并已定向清理测试记录。

### 验证

- `systemctl is-active open-resouce-gateway nginx` 均为 `active`。
- Gateway `/healthz` 与 MySQL 感知的 `/readyz` 均返回 200。
- Nginx 本机首页返回 200，`/api/v1` 返回 Gateway `v1` 服务信息。
- 前端 TypeScript 与 Vite 生产构建通过；Gateway `go vet` 和 Linux/amd64 编译通过。
- OpenAPI 契约校验通过。

### 卡点与注意事项

- 从开发机直接访问 `103.236.98.166:80` 或 `www.openresource.cn:80` 得到的不是服务器 Nginx 响应，而是上游 `404 NOTOK`；服务器本机 Nginx 验证正常且防火墙未启用。
- 公网入口需要在云厂商控制台检查安全组、端口映射或备案拦截。当前 443 尚未配置，HTTPS 不可用。
- 备案审核期间曾尝试 `8080` 明文开发入口，但服务商对公网 IP 的非标准端口 HTTP 同样实施备案拦截，因此未保留该监听。
- 增加 `8443` 自签名 HTTPS 开发入口，可通过公网 IP 联调完整前端与同源 `/api`；仅用于备案期间调试。
- 前端生产构建提示主 JavaScript chunk 约 562 KB，功能不受影响，后续应按路由拆包。

### 下一步

1. 修复云侧 80/443 公网入口并配置 TLS 证书。
2. 增加可重复执行的发布脚本和 Gateway 启动就绪重试。
3. 开始文档仓库 MongoDB 接入或继续完善评论回复接口。

## 2026-07-28：文档评论回复闭环

### 已完成

- 新增 `POST /api/v1/projects/{slug}/documents/{documentSlug}/comments/{commentID}/replies`。
- 评论响应增加 `parent_id` 与嵌套 `replies`，列表按根评论聚合一层回复。
- MySQL 回复创建使用事务锁定父评论，校验父评论属于当前文档，并继承稳定块锚点。
- 内存仓库与 MySQL 仓库统一支持回复创建、父评论不存在和嵌套读取。
- 前端“回复”按钮接入真实 API，增加回复输入、提交状态和线程内回复展示。
- OpenAPI 更新为 10 条路径、22 个 schema。

### 验证

- `go vet ./services/gateway` 通过。
- Linux 全量普通测试通过。
- 真实 MySQL 集成测试在允许来源的应用服务器运行通过，覆盖父评论、回复、嵌套列表、解决与清理。
- TypeScript 和 Vite 生产构建通过。
- OpenAPI 契约校验通过。
- 新版 Gateway 与前端已部署到 `https://103.236.98.166:8443`。
- 公网端到端回归通过：创建父评论、创建回复、嵌套列表读取均成功，回归数据已清理。
- 公网首页已加载本次构建的 `index-DnEXq7T_.js`。

### 下一步

1. 增加回复删除和评论线程计数。
2. 接入实时事件推送，替换前端当前的静态“实时同步中”状态。
3. 增加登录用户身份后移除前端临时作者“我”。

## 2026-07-28：回复软删除与线程计数

### 已完成

- 新增 `DELETE /api/v1/projects/{slug}/documents/{documentSlug}/comments/{commentID}/replies/{replyID}`。
- 删除操作校验文档、父评论和回复关系，MySQL 使用 `deleted_at` 软删除。
- 评论响应新增 `reply_count`，由当前未删除回复聚合得出。
- 前端文档讨论区显示线程数和回复总数。
- 当前临时作者“我”的回复显示删除入口，并处理 204 空响应和删除中状态。

### 验证

- Go 静态检查和 Linux 全量普通测试通过。
- 真实 MySQL 集成测试通过，覆盖首次删除、重复删除、软删除后列表过滤与计数归零。
- TypeScript、Vite 生产构建和 OpenAPI 契约校验通过。
- OpenAPI 当前包含 11 条路径和 22 个 schema。
- 新版前后端已部署到公网 8443 开发入口。
- 公网端到端回归确认回复计数从 1 变为 0、首次删除返回 204、重复删除返回 404，测试数据已清理。

### 下一步

1. 接入实时事件推送。
2. 增加登录身份与评论权限校验。
3. 为评论和回复增加编辑能力。

## 2026-07-28：评论实时事件推送

### 已完成

- 新增文档评论 SSE 事件流 `GET /api/v1/projects/{slug}/documents/{documentSlug}/comments/events`。
- 评论创建、回复创建、回复删除和评论解决后发布文档级变更事件。
- 事件流增加连接确认、15 秒心跳、断连订阅清理与 Nginx 禁止缓冲响应头。
- 前端使用 `EventSource` 订阅当前文档，收到事件后重新读取评论列表，并随文档切换清理旧订阅。
- OpenAPI 增加 SSE 路径和事件类型说明。

### 验证

- Go 静态检查、Linux 全量测试、TypeScript、Vite 生产构建和 OpenAPI 校验通过。
- 新版前后端已部署到公网 8443 开发入口。
- 公网 SSE 回归收到 `comment.created`、`reply.created`、`reply.deleted`、`comment.resolved` 的完整顺序，测试数据已清理。

## 2026-07-28：评论与回复编辑

### 已完成

- 根评论 `PATCH` 接口支持以 `body` 编辑正文，并与 `status` 解决操作进行严格互斥校验。
- 回复资源新增 `PATCH` 编辑接口；内存仓库和 MySQL 仓库统一实现正文更新。
- 评论响应新增 `updated_at`，前端根据创建与更新时间显示“已编辑”。
- 前端为当前临时作者“我”的评论和回复提供行内编辑、保存状态与失败重试提示。
- 编辑成功后发布 `comment.updated` 或 `reply.updated` SSE 事件。
- MySQL 连接启用 `clientFoundRows`，相同正文的幂等更新仍可正确识别目标资源存在。

### 验证

- `go vet ./services/gateway`、Linux 全量普通测试、TypeScript 和 Vite 生产构建通过。
- 真实 MySQL 集成测试实际执行并通过，覆盖根评论编辑、回复编辑、列表读取和清理。
- OpenAPI 契约校验通过，当前包含 12 条路径和 23 个 schema。
- 新版 Gateway 与前端已部署到 `https://103.236.98.166:8443`，前端资源为 `index-CTGgCvC-.js`。
- 公网端到端回归确认评论和回复编辑均返回 200，列表返回更新后的正文和 `updated_at`；测试数据已清理。

### 下一步

1. 实现登录身份与服务端评论作者绑定。
2. 只允许作者编辑或删除自己的内容，并为解决评论定义权限。
3. 增加认证接口限流与安全审计。

## 2026-07-28：邮箱身份、会话与评论权限边界

### 已完成

- 新增注册、登录、退出和当前用户接口：`POST /api/v1/auth/register`、`POST /api/v1/auth/login`、`POST /api/v1/auth/logout`、`GET /api/v1/auth/me`。
- 新增 `users` 与 `auth_sessions` MySQL 表，并将评论 `author_id` 外键连接到用户表。
- 密码使用随机盐和 PBKDF2-HMAC-SHA256（210,000 轮）保存，不存储明文。
- 登录令牌只保存 SHA-256 摘要；浏览器使用 `Secure`、`HttpOnly`、`SameSite=Lax` Cookie，JavaScript 无法读取令牌。
- 评论与回复作者由服务端当前会话绑定，忽略客户端伪造的作者名称。
- 评论编辑、解决以及回复编辑、删除仅允许内容作者执行；跨用户操作返回 404，避免泄露资源归属。
- 前端登录弹窗接入真实邮箱注册与登录，启动时恢复会话，页头展示当前昵称并支持退出。
- 前端评论操作入口根据 `author_id` 和当前用户 ID 展示，移除基于作者文字“我”的临时权限判断。
- 跨域开发请求支持凭据，CORS 补齐 `DELETE` 与 `Access-Control-Allow-Credentials`。

### 验证

- Go 静态检查、Linux 全量普通测试、TypeScript、Vite 生产构建和 OpenAPI 契约校验通过。
- 真实 MySQL 认证仓库和评论仓库集成测试均实际执行并通过。
- `000002_create_users_and_sessions` 已应用到 `open_resouce` 数据库。
- 新版前后端已部署到 `https://103.236.98.166:8443`，前端资源为 `index-ASs4NXMk.js`。
- 公网双账号回归通过：匿名写入返回 401，安全 Cookie 生效，作者由服务端绑定，跨用户编辑和删除返回 404，本人编辑与回复操作成功，退出后 `/auth/me` 返回 401，重新登录成功。
- 公网回归创建的评论、会话和用户数据已清理。

### 下一步

1. 为注册与登录接口增加按 IP 和邮箱维度的频率限制。
2. 增加会话列表、主动失效全部会话和密码修改能力。
3. 增加结构化安全审计日志，记录认证失败和越权尝试。

## 2026-07-28：认证频率限制与安全审计

### 已完成

- 注册接口按真实客户端 IP 和邮箱摘要限制为每小时 5 次。
- 登录接口按真实客户端 IP 和邮箱摘要限制为每 15 分钟 10 次。
- 超限响应返回 HTTP 429、`rate_limited` 错误码和 `Retry-After`。
- Nginx 固定覆盖 `X-Forwarded-For` 为公网连接地址，阻止客户端伪造转发链绕过限流。
- 注册成功/冲突、登录成功/失败、匿名写操作和评论越权操作写入结构化安全审计日志。
- 审计日志只记录邮箱短摘要，不记录邮箱明文、密码或会话令牌。

### 验证

- Go 静态检查、Linux 全量测试和 OpenAPI 校验通过。
- 公网连续 10 次无效登录返回 401，第 11 次返回 429。
- 每次请求携带不同的伪造 `X-Forwarded-For` 仍触发同一真实来源限流，证明 Nginx 覆盖配置生效。
- 验证结束后已重启 Gateway 清空测试限流窗口，`/readyz` 返回 200。

### 下一步

1. 增加用户主动查看和失效全部登录会话的能力。
2. 增加密码修改和忘记密码流程。
3. 将单机内存限流迁移到 Redis，以支持多实例一致限流。

## 2026-07-28：根评论软删除与第三方登录恢复

### 已完成

- 根评论资源新增 `DELETE`，只允许线程作者删除。
- MySQL 在事务中软删除根评论及全部回复；内存仓库保持相同行为。
- 前端作者操作区增加线程删除入口，并同步移除本地线程。
- 恢复 GitHub 与微信登录主入口，邮箱登录保留为补充方式。
- 新增 GitHub/微信 OAuth 开始与回调接口、随机 state 防伪 Cookie、服务端令牌交换、第三方身份映射和本站 HttpOnly 会话创建。
- GitHub 流程按官方 Web Application Flow 使用授权码换取令牌并重新读取用户身份。
- OAuth 客户端密钥只从服务端环境变量读取，不进入前端构建或仓库。

### 验证

- Go 静态检查、Linux 全量测试、TypeScript、Vite 构建和 OpenAPI 校验通过。
- 根评论删除的内存仓库、MySQL 集成测试和双用户权限测试通过。
- 最新前后端已部署到公网开发入口，前端资源为 `index-Bx7LcbN4.js`。
- GitHub/微信未配置时均明确返回 HTTP 503 和 `oauth_not_configured`，不会伪装登录成功。
- 公网回归临时用户和评论数据已清理。

### 第三方凭据联调补记

- GitHub OAuth App 的 Client ID/Secret 已写入应用服务器 `0600 root:root` 环境文件，未写入仓库或前端资源。
- `PUBLIC_BASE_URL` 当前设置为 `https://103.236.98.166:8443`。
- 公网授权入口返回 GitHub 302，回调地址为 `https://103.236.98.166:8443/api/v1/auth/oauth/github/callback`，GitHub 授权页接受当前 Client ID。
- GitHub 完整授权回调已成功：第三方身份已映射为本站用户，创建一条有效 HttpOnly 会话，回调返回 302 至首页。
- 应用服务器到 GitHub 的国际链路曾发生数次 30 秒连接超时，随后一次回调在约 4 秒内成功；Gateway 当前就绪，后续应为 OAuth 出站访问配置更稳定的网络路径。
- 微信因开放平台认证费用暂不配置；登录产品形态调整为 GitHub 主登录、邮箱补充登录，后续可增加 Passkey。

## 2026-07-28：多设备会话撤销

### 已完成

- 认证仓库新增按用户删除全部会话能力，内存与 MySQL 实现保持一致。
- 新增 `POST /api/v1/auth/logout-all`，要求有效登录态并清除当前 Cookie。
- 前端页头昵称改为账号菜单，提供“退出当前设备”和“退出所有设备”。
- 全部会话撤销写入结构化安全审计事件 `all_sessions_revoked`。

### 验证

- Go 静态检查、Linux 全量测试、真实 MySQL 集成测试、TypeScript、Vite 和 OpenAPI 校验通过。
- 最新前后端已部署，前端资源为 `index-Bs0AzlHK.js`。
- 公网临时账号建立两个独立 Cookie 会话，两边 `/auth/me` 均返回 200。
- 调用“退出所有设备”返回 204，随后两个会话均返回 401；临时账号已清理。

## 2026-07-29：活跃会话查看与单设备撤销

### 已完成

- 新增 `GET /api/v1/auth/sessions`，仅返回当前用户尚未过期的登录会话，并标记当前 Cookie 对应的会话。
- 新增 `DELETE /api/v1/auth/sessions/{sessionID}`，会话删除同时校验用户归属，避免跨账号枚举或撤销。
- MySQL 单会话撤销使用事务和 `FOR UPDATE` 锁定目标记录；撤销当前会话时同步清除浏览器 Cookie。
- 前端账号菜单展示活跃会话数量、创建时间和当前设备标记，可分别退出任意设备。
- 单会话撤销写入结构化安全审计事件 `session_revoked`。
- OpenAPI 更新为 21 条路径、29 个 schema。

### 验证

- 本地 Go 全量测试、TypeScript、Vite 生产构建和 OpenAPI 契约校验通过。
- 在应用服务器使用真实 MySQL 配置运行全量测试通过，覆盖会话列表、当前会话识别、事务级单会话删除和全部会话撤销。
- 新版前后端已部署到 `https://103.236.98.166:8443`，前端资源为 `index-BnwTEZsF.js`。
- 公网双会话回归通过：会话列表返回 2 条且只有 1 条标记为当前；撤销另一条返回 204；当前会话 `/auth/me` 保持 200，被撤销会话立即返回 401。
- 公网回归临时账号及其会话已从 MySQL 清理。

### 下一步

1. 增加邮箱账号修改密码能力，修改成功后撤销其他设备会话。
2. 设计忘记密码流程所需的邮件发送和一次性令牌表。
3. 将单机内存认证限流迁移到 Redis，以支持多实例一致限流。

## 2026-07-29：邮箱账号修改密码

### 已完成

- 新增 `PUT /api/v1/auth/password`，要求有效会话、正确的当前密码以及 8～128 位新密码。
- 密码更新与“撤销其他设备会话”在同一个 MySQL 事务中完成；当前设备会话继续有效。
- 当前密码错误和密码修改成功分别记录 `password_change_failed`、`password_changed` 安全审计事件。
- 用户响应新增 `has_password`，前端据此只向邮箱账号显示修改密码表单；GitHub 登录账号显示无需站内密码。
- 修改密码入口已集成到账户菜单，成功后立即刷新本地会话列表。
- OpenAPI 更新为 22 条路径、30 个 schema。

### 验证

- Linux 全量测试与真实 MySQL 集成测试通过，覆盖错误旧密码、密码持久化、保留当前会话和撤销其他会话。
- TypeScript、Vite 生产构建和 OpenAPI 契约校验通过。
- 新版前后端已部署到 `https://103.236.98.166:8443`，前端资源为 `index-q-ACy_cX.js`。
- 公网回归结果：修改密码返回 204；当前会话 `/auth/me` 返回 200；另一会话返回 401；旧密码登录返回 401；新密码登录返回 200。
- 公网回归临时账号及其会话已从 MySQL 清理。

## 2026-07-29：Redis 分布式认证限流

### 已完成

- 引入官方 `github.com/redis/go-redis/v9` 客户端，认证限流在配置 `REDIS_URL` 后切换为 Redis 固定窗口实现。
- 使用 Lua 脚本原子执行计数、自首请求设置过期时间并读取剩余 TTL，避免 `INCR` 与 `EXPIRE` 分离产生无过期键。
- 注册、登录和修改密码继续同时按真实来源 IP 与身份摘要限流，键统一使用 `open-resouce:auth-limit:` 前缀。
- Redis 连接在 Gateway 启动时强制探活；启用后 `/readyz` 同时检查 MySQL 与 Redis。
- 应用服务器环境文件新增 `REDIS_URL`，仍保持 `0600 root:root`，真实密码未写入仓库。
- Redis 主机 firewalld 仅允许应用服务器 `103.236.98.166/32` 访问 TCP 6379，未向其他公网来源开放。

### 验证

- 本地 Go 测试和 Linux/amd64 构建通过。
- 在应用服务器使用真实 MySQL 与 Redis 运行全量集成测试通过。
- Redis 密码认证返回 `PONG`，应用服务器到 Redis 的白名单连接验证通过。
- Gateway 已部署并记录 `redis authentication rate limiter enabled`，服务状态为 `active`，`/readyz` 返回 200。
- 重启持久性回归：重启前 5 次无效登录返回 401；重启后第 6～10 次继续返回 401，第 11 次返回 429；Redis 的 IP 与身份键计数均为 11。
- 回归使用的 Redis 键已定向删除，未影响真实用户限流状态。

## 2026-07-29：项目收藏持久化

### 已完成

- 新增 `project_favorites` MySQL 表，以用户和项目联合主键保证收藏写入幂等，并通过用户外键级联清理。
- 新增 `GET /api/v1/favorites`、`POST /api/v1/projects/{slug}/favorite` 和 `DELETE /api/v1/projects/{slug}/favorite`。
- 收藏接口要求有效登录态，服务端校验项目存在；重复收藏和重复取消均返回 204。
- 前端移除仅存在内存中的临时收藏行为，登录后从服务端恢复收藏状态。
- 未登录点击收藏会打开登录窗口；收藏写入采用乐观更新，接口失败时自动回滚 UI。
- OpenAPI 更新为 24 条路径、31 个 schema。

### 验证

- `000003_create_project_favorites` 已应用到生产 MySQL，表引擎为 InnoDB。
- 本地 Go 测试、TypeScript、Vite 生产构建和 OpenAPI 校验通过。
- 真实 MySQL + Redis 全量集成测试通过，覆盖收藏幂等写入、列表、取消、未登录拦截和未知项目。
- 新版前后端已部署到 `https://103.236.98.166:8443`，前端资源为 `index-B3k_q3yN.js`。
- 公网跨会话回归：会话 A 收藏后会话 B 读取到 `atlas`；Gateway 重启后收藏仍存在；会话 B 取消后会话 A 读取到空列表。
- 回归临时用户、会话、收藏和对应 Redis 限流键均已清理。

## 2026-07-29：用户昵称编辑与我的收藏视图

### 已完成

- `GET /api/v1/auth/me` 同一路径新增 `PATCH`，支持登录用户修改 2～80 字符的显示昵称。
- 内存和 MySQL 认证仓库均实现昵称持久化；更新结果立即返回完整安全用户视图。
- 昵称修改记录 `profile_updated` 结构化审计事件。
- 前端账户菜单新增昵称编辑、输入校验、保存状态和失败提示。
- 账户菜单新增“查看我的收藏”，进入后项目目录仅显示当前账号收藏的项目。
- OpenAPI 保持 24 条路径，schema 增加到 32 个。

### 验证

- 本地 Go 测试、真实 MySQL + Redis 全量测试、TypeScript、Vite 和 OpenAPI 校验通过。
- 公网双会话回归：会话 A 修改昵称返回 200，会话 B 随后读取 `/auth/me` 获得相同新昵称。
- 最新前端已部署到公网开发入口，资源为 `index-BXkNuOha.js`。
- 昵称回归临时用户、会话和 Redis 限流键已清理。

## 2026-07-29：邮箱找回密码

### 已完成

- 新增 `password_reset_tokens` 表，仅保存令牌 SHA-256 摘要，令牌 30 分钟过期且只能消费一次。
- 新增 `POST /api/v1/auth/password-reset/request`，对存在和不存在的邮箱统一返回 202，避免账户枚举。
- 新增 `POST /api/v1/auth/password-reset/confirm`，密码更新、令牌消费和全部会话撤销在同一 MySQL 事务中完成。
- 找回和确认接口接入 Redis 分布式限流，并记录结构化安全审计事件。
- 使用腾讯企业邮 SMTP 465 隐式 TLS 发送 UTF-8 重置邮件，凭据仅保存在服务器 `0600 root:root` 环境文件。
- 前端登录窗口新增“忘记密码”和重置密码视图，可从邮件 URL 的 `reset_token` 查询参数直接进入。
- OpenAPI 更新为 26 条路径、35 个 schema。

### 验证

- `000004_create_password_reset_tokens` 已应用到生产 MySQL。
- 本地 Go 全量测试、真实 MySQL + Redis 全量测试、前端 lint、TypeScript、Vite 构建和 OpenAPI 校验通过。
- 应用服务器到 `smtp.exmail.qq.com:465` 的真实 TLS 认证和投递测试通过。
- Gateway 重启后为 `active`，内网 `/healthz` 和同时探测 MySQL、Redis 的 `/readyz` 均返回 200。
- 新版前后端已部署到 `https://103.236.98.166:8443`，前端资源为 `index-DGn2OqMZ.js`。
- 公网未知邮箱请求返回 202；临时账户测试确认腾讯企业邮不支持该邮箱的加号子地址，并已清理临时用户、会话和重置令牌。

## 2026-07-29：P0 项目发布审核与 OSS 文件直传

### 已完成

- 新增作者项目草稿、编辑、删除、列表和提交审核接口；项目状态覆盖草稿、待审核、已发布、已驳回和已下架。
- 新增管理员待审核列表、审核通过和驳回接口；管理员由服务端 `ADMIN_EMAILS` 白名单判定。
- 审核状态变更和审核事件在同一个 MySQL 事务中写入，未审核项目不会进入公开项目目录。
- 前端账号菜单新增作者项目中心；管理员账号额外显示项目审核中心。
- 作者项目表单支持封面、PDF/Markdown 文档和 zip/tar/gzip 代码包。
- 文件由浏览器使用十分钟有效的 V4 预签名 URL 直传阿里云 OSS，不经过应用服务器，也不向前端暴露 AccessKey。
- 已发布项目的文档和代码包通过 Gateway 生成短时下载 URL，并以 307 重定向到私有 OSS 对象。
- 图片最大 10 MB、文档最大 50 MB、代码包最大 500 MB；服务端同时校验资源种类、MIME、扩展名和对象归属前缀。
- OSS Bucket CORS 仅允许公网测试入口和正式域名执行 PUT、GET、HEAD。
- OpenAPI 更新为 33 条路径、43 个 schema。

### 验证

- `000005_create_managed_projects` 已应用到生产 MySQL。
- Linux 全量测试通过；真实 MySQL、Redis、项目审核事务和 OSS 上传/删除集成测试通过。
- OSS 临时测试对象在验证后已删除。
- 公网回归通过：注册作者、获取 OSS 授权、CORS 预检、创建草稿、提交审核、作者列表均成功。
- 公网回归临时用户和关联项目、审核数据已级联清理。
- 新版前后端已部署到 `https://103.236.98.166:8443`，前端资源为 `index-BYJiSqnh.js`。

## 2026-07-29：在线 Markdown 文档、完整 Emoji 与协作编辑演进

### 已完成

- 作者项目中心由表单优先改为在线文档式工作区，项目标题、摘要和正文成为主编辑区，发布元数据收纳到折叠设置中。
- 接入 MIT 许可的 Milkdown Crepe，默认提供所见即所得 Markdown 编辑，保留 Markdown 源码、分屏和只读预览模式；正文仍以 Markdown 字符串持久化，不使用编辑器私有 JSON。
- 富文本编辑器支持标题、强调、引用、列表、任务列表、链接、代码块、表格、公式、撤销和块级编辑。
- Mermaid 代码块支持实时图表渲染；编辑器外部工具栏可直接插入图表模板。
- 图片和附件继续直传私有 OSS。Milkdown 图片节点通过 Gateway 的作者预览地址展示 `oss://` 图片，保存时仍保留稳定对象键；公开项目使用发布态资源地址。
- 草稿与驳回项目在内容变更后自动保存，仍保留显式“保存草稿”和“提交审核”动作。
- 使用 MIT 许可的 Emoji Mart 替换 16 个固定 Emoji，支持完整分类、中文搜索、最近使用、肤色和原生 Unicode；作者编辑、评论、回复及评论编辑共用同一选择器。
- Emoji Mart、Milkdown、Mermaid 均按需加载；首页主样式不包含 Milkdown 的 79 KB 编辑器样式。
- 手机宽度下修复富文本工具栏横向溢出和 Emoji 弹层裁切。
- 多人协作技术路线确定为 Milkdown `@milkdown/plugin-collab` + Yjs；后续自托管 WebSocket 同步层，正文继续以 Markdown 快照作为审核和发布的稳定边界。
- OpenAPI 当前为 35 条路径、43 个 schema。

### 验证

- 前端 `oxlint`、TypeScript 和 Vite 生产构建通过；Go 测试二进制编译通过，本机执行被 Windows Application Control 拦截，最近一次 Linux 全量测试仍通过。
- 使用公网真实 Gateway 回归临时账号登录，验证作者中心、Milkdown 加载、Mermaid 插入、完整 Emoji 搜索/分类、Emoji 写入正文和评论区 Emoji 面板。
- 回归临时账号及会话已从生产 MySQL 定向删除。
- 使用 390 × 844 视口回归手机布局，编辑器宽度不再超过容器。
- 新版前端已部署到 `https://103.236.98.166:8443`，发布目录为 `20260729221940`，主资源为 `index-ScUH3VbQ.js`。
- Nginx 配置检查和 reload 成功；Gateway 为 `active`，服务器内网 `/healthz`、`/readyz` 均返回正常，公网 `/api/v1` 与项目列表返回 200。

## 2026-07-29：作者富文本弹窗滚动修复

- 作者项目中心打开时锁定 `body` 滚动，并补偿滚动条宽度，避免底层页面移动和布局抖动。
- 弹窗遮罩、作者中心和编辑表单启用 `overscroll-behavior: contain`，编辑表单成为唯一纵向滚动容器。
- 作者中心关闭或卸载时恢复原有页面滚动样式。
- 浏览器回归确认：滚轮前后底层 `window.scrollY` 保持 0，编辑表单 `scrollTop` 从 0 增加到 155.5；关闭后 `body` 恢复可滚动。
- 回归临时账号及会话已定向清理。
- 修复已部署到公网测试入口，发布目录为 `20260729223523`，主资源为 `index-BXGw3q-3.js`。

## 2026-07-29：真实长文档滚动修复

- 真实长文档回归发现：Milkdown 正文实际高度约 8203 px，但富文本外层被固定在 430 px 且使用 `overflow: hidden`，导致长正文被裁切，外层表单仍只有 155 px 可滚范围。
- 富文本外层改为允许内容参与高度计算，富文本模式工作区不再以压缩型 flex 高度固定；正文、工作区和后续项目设置按正常文档流顺序排列。
- 移除临时的 React 滚轮捕获接管，避免浏览器原生滚动与手动修改 `scrollTop` 同时发生而产生双倍位移或被动监听警告。
- 页面锁定继续使用固定定位，同时锁定根节点与 `body`；关闭作者中心时恢复原样式和原页面位置，隔离文档滚动与底层页面滚动。
- 使用 60 节长 Markdown 文档真实回归：富文本正文和工作区从 430 px 正确扩展到 6731 px，外层文档可滚范围达到 6458 px。
- 滚轮单次移动 1200 px 时表单仅移动 1200 px；可继续滚到第 60 节、项目设置和底部保存按钮，底层 `window.scrollY` 全程保持 0。
- 两轮回归临时账号及会话均已从生产 MySQL 定向删除。
- 前端 `oxlint`、TypeScript 和 Vite 生产构建通过。
- OSS 的 AccessKey、Secret、Bucket 和 Endpoint 均已在服务端环境配置中确认存在，未写入前端构建。
- 修复已部署到公网测试入口，发布目录为 `20260729150020`，主资源为 `index-H0WMBqC_.js`，主样式为 `index-CgaTZsgx.css`。
- Nginx 配置检查和 reload 成功；Gateway 为 `active`，内网 `/healthz`、`/readyz` 正常，公网 `/api/v1` 返回 200。

## 2026-07-29：已发布文档多人实时协作

### 已完成

- 项目详情页新增“协作编辑”入口，项目所有者和拥有 `editor` 权限的成员可直接编辑已发布 Markdown 文档。
- 项目所有者可按已注册邮箱添加协作者、切换 `editor` / `viewer` 角色和移除成员；降级或移除会立即断开该成员的活跃协作连接。
- Milkdown collaboration plugin 与 Yjs 接入自托管 WebSocket，同步正文更新和 Awareness 光标状态；编辑器显示在线人数、成员头像、远程光标姓名和保存修订号。
- 新增 `project_collaborators` 与 `project_collaboration_snapshots` 表；Yjs 快照和公开 Markdown 正文同步持久化。
- WebSocket 握手复用 HttpOnly 会话并执行项目级权限校验、同源校验、消息大小限制和单连接串行写入。
- Nginx 增加协作路径的 Upgrade 转发和一小时读写超时；公网 IP 的 8443 测试入口保留完整 Host 端口以通过同源校验。
- OpenAPI 补充协作权限、协作者管理和 WebSocket 握手契约。

### 验证

- 后端权限生命周期测试与 WebSocket 初始化、Yjs 快照、公开 Markdown 持久化测试通过。
- 前端 lint、TypeScript 与 Vite 生产构建通过。
- 公网双账号真实回归通过：所有者和编辑者双方均显示 2 人在线；所有者端检测到 1 个远程光标；第二位编辑者输入的内容实时出现在所有者端并保存为修订 3。
- 公开项目详情接口读取到实时编辑后的 Markdown，确认协作内容已持久化而非仅停留在浏览器内存。
- 最终版本已部署到公网测试入口，发布目录为 `20260729153905`，主资源为 `index-CrkNFHyK.js`，协作编辑器资源为 `CollaborativeMarkdownEditor-CkKQpLuk.js`。
- Gateway 与 Nginx 均为 `active`，内网 `/healthz`、`/readyz` 和公网 `/api/v1` 返回正常；双账号回归项目、账号、会话、协作者与快照已定向清理。

## 2026-07-31：站内通知与实时推送

### 已完成

- 新增 `notifications` MySQL 迁移 `000007_create_notifications`，包含接收者索引、未读索引和用户外键级联清理。
- 新增 `GET /api/v1/notifications`，返回当前用户最近 50 条通知和未读数量。
- 新增 `POST /api/v1/notifications/{notificationID}/read` 与 `POST /api/v1/notifications/read-all`，已读操作幂等；跨用户标记返回 404。
- 新增 `GET /api/v1/notifications/events` SSE 通知流，按登录用户订阅，含连接确认、15 秒心跳和断连清理。
- 通知仓库提供内存与 MySQL 双实现；`DATABASE_URL` 存在时自动切换 MySQL。
- 触发点一：评论被回复后通知根评论作者（`comment.replied`），携带项目、文档、评论锚点和最多 120 字回复摘要；自己回复自己不产生通知。
- 触发点二：管理员审核后通知项目作者（`project.approved` / `project.rejected`），驳回通知携带驳回原因。
- 通知发送失败仅记录日志，不阻塞评论和审核主流程。
- 前端页头铃铛接入真实通知：未读徽标（99+ 封顶）、通知面板、单条点击已读、全部已读；未登录点击引导登录。
- 前端登录后自动加载通知并建立 SSE 订阅，收到事件后刷新列表；退出登录时清空通知状态并关闭订阅。
- OpenAPI 更新为 43 条路径、51 个 schema，新增 Notification 与 NotificationListResponse。

### 验证

- 本机 Go 1.24.6 `go vet` 和 Gateway 全量测试通过。
- 新增 6 组通知测试全部通过：匿名拦截、回复通知、自回复不通知、单条/全部已读与跨用户 404、审核通过/驳回通知、事件中心按用户隔离。
- Linux/amd64 静态交叉编译成功。
- 前端 oxlint、TypeScript 和 Vite 生产构建通过。
- OpenAPI 契约校验通过。

### 卡点与注意事项

- 真实 MySQL 通知仓库集成测试需在允许来源的应用服务器上补做（应用账号限制了来源 IP）。
- 审核通知在内存仓库模式下同样生效；通知不阻塞主流程，即使通知表缺失也不会影响评论和审核接口返回。
- 通知点击后暂不跳转到对应文档或项目，仅标记已读；路由化跳转作为后续独立功能。
- 评论解决当前只允许作者本人操作，因此未产生“评论被解决”通知；当项目作者获得解决权限后再补充该通知类型。

## 2026-08-01：站内通知上线部署

### 已完成

- `000007_create_notifications` 已应用到中间件服务器 `www.lovenuaa.xyz` 的 `open_resouce` 库（MySQL 5.7.39），表引擎 InnoDB、字符集 utf8mb4、用户外键级联正确。
- 应用账号为库级授权 `open_resouce.*`，新表自动覆盖，无需额外授权。
- 应用服务器 `103.236.98.166` 安装 mysql 8.0.26 客户端，用于从允许来源 IP 连接中间件服务器执行迁移。
- Gateway 新二进制已原子替换并重启，上一版本保留为 `gateway.previous` 用于回退。
- 前端已发布，发布目录 `20260801030152`，主资源 `index-Cp1ezW--.js`，主样式 `index-Cl_n_Y-S.css`。
- 新增通知仓库真实 MySQL 集成测试，覆盖倒序列表、未读计数、幂等已读不覆盖首次时间、跨用户隔离和外键级联清理。

### 验证

- 应用服务器上使用真实 MySQL + Redis 运行全量测试通过，无跳过用例（补齐 `TEST_MYSQL_DSN` 后项目审核集成测试也已实际执行）。
- `TestMySQLNotificationRepositoryIntegration` 在真实 MySQL 上通过。
- 内网 `/healthz`、同时探测 MySQL 与 Redis 的 `/readyz` 均返回 200；Gateway 与 Nginx 均为 `active`。
- 公网端到端回归 16 项全部通过：双账号注册、初始未读 0、创建评论、异账号回复后作者未读为 1、通知类型与标题中含回复人昵称、project_slug 正确、回复者本人无通知、跨用户标记 404、已读 204 且幂等、全部已读 204、匿名访问通知与 SSE 均 401。
- SSE 实时推送已在公网验证：订阅端收到 `event: notification` 且载荷为 `"type":"comment.replied"`。
- 中文存储已用 HEX 字节级确认正确（例如 `E9A1B9E79BAEE99B86E68890E6B58BE8AF95` = “项目集成测试”）；mysql 客户端经 SSH 通道输出的 `?????` 仅为 locale 显示问题，不是数据问题。
- 回归临时账号、会话、评论和通知已定向清理；服务器上上传的测试二进制、前端包和回归脚本已删除。

### 修复：集成测试泄漏测试账号到生产库

- 根因：`auth_mysql_test.go`、`favorites_test.go`、`managed_projects_mysql_test.go` 使用 `defer database.Close()`。Go 的 `defer` 在函数 return 时执行，而 `t.Cleanup` 在之后才执行，因此清理 SQL 在连接已关闭时运行，错误又被 `_, _ =` 忽略，导致历次“临时账号已清理”的记录实际从未生效。
- 影响：自 2026-07-28 累积 25 个测试账号（共 27 个用户，仅 2 个真实用户）。其中 2 个名为“MySQL Project”的测试项目处于 `published` 状态，已出现在公网项目目录首位，目录总数被抬到 6。
- 修复：三个文件改为用 `t.Cleanup` 注册关闭（LIFO 保证删除先于关闭），并将清理错误从静默忽略改为 `t.Errorf` 上报。新增的通知集成测试同步采用正确写法。
- 验证：修复后重跑全部 MySQL 集成测试，`@example.com` 账号数量保持 25 不变（修复前每轮新增 3 个）。
- 已清理 25 个累积测试账号及其级联测试项目；删除前逐项校验为测试 ID 模式加 `@example.com` 邮箱，2 个真实用户和 1 个真实项目 `my-project` 未受影响。
- 清理后公网项目目录恢复为 4 个正常项目。

### 注意事项

- 应用服务器 sshd 显式设置 `PubkeyAuthentication no`，公钥免密不可用，本次部署使用密码认证（密码仅通过环境变量传入，未写入仓库）。未改动服务器安全策略；如需长期免密部署，需项目负责人决定是否开启公钥认证。
- 本地开发机无法直连中间件 MySQL（应用账号限定来源 IP 为 103.236.98.166），数据库迁移和集成测试必须在应用服务器上执行。
- 集成测试直连生产库，依赖测试自身清理。后续应考虑改用独立测试库，避免清理遗漏直接污染生产数据。
- 域名备案中，当前入口仍为公网 IP 的 8443 自签 HTTPS。

### 下一步

1. 通知点击跳转到对应项目文档或作者中心。
2. 为集成测试配置独立测试数据库，与生产库隔离。
3. 补齐审核通过/驳回的公网端到端回归（本次已由内存与 MySQL 测试覆盖，尚未在公网走真实审核流程）。

## 2026-08-01：代码包在线浏览

### 背景

一期验收总标准第 6 条要求“用户可以查看代码高亮内容”，需求 6.7 要求代码目录树、文件查看、高亮、路径与大小、复制、文件名搜索和 README 优先。
之前“代码预览”页是完全硬编码的 `atlas / runtime.py`，任何项目都显示同样内容（已在 2026-07-26 记录为卡点），本次补齐。

### 已完成

- 对象存储接口新增 `GetObject`，支持带上限读取，超限返回可识别的错误。
- 新增 `GET /api/v1/projects/{slug}/code` 返回代码目录树，目录条目由文件路径推导补齐，按层级、目录优先、字典序排列。
- 新增 `GET /api/v1/projects/{slug}/code/file?path=` 返回单个文本文件，带语言标识用于高亮。
- 支持 zip、tar.gz 和 tar 三种格式，按魔术字节优先、扩展名兼容识别。
- 目录树支持 `q` 按路径模糊搜索，命中后只返回文件条目。
- README 自动识别并优先展示，根目录优先、其次路径最短。
- 归档内存缓存按对象键缓存，10 分钟 TTL，总量上限 192 MB，超量时淘汰最旧条目。
- 前端代码预览页改为真实 API 驱动：目录树、文件名搜索（250ms 防抖）、文件大小、完整路径、行号、复制代码、下载代码包、加载与失败状态。
- 移除前端硬编码的 `codeFiles` 示例数据。
- OpenAPI 更新为 45 条路径、55 个 schema。

### 安全防护

- 路径穿越（zip slip）：归档内路径和请求参数均经统一归一化，拒绝 `..`、绝对路径、反斜杠、空字节和过长路径。
- zip 炸弹：解压累计读取上限 256 MB，gzip 流同样受限；条目数上限 2000。
- 可浏览归档上限 64 MB，超过时返回 413 并提示下载，不将大文件载入内存。
- 单文件预览上限 512 KB，超出截断并标记 truncated。
- 非 UTF-8 或含空字节的二进制文件拒绝预览，返回 415。
- 仅已发布项目可浏览，未发布项目返回 404。

### 验证

- `gofmt`、`go vet`、本地全量测试通过；新增 10 组代码浏览测试全部通过，覆盖 zip/tar.gz 目录与文件、搜索、路径穿越、二进制拒绝、大文件截断、未发布项目、错误方法、路径归一化、缓存过期淘汰和语言识别。
- 应用服务器真实 MySQL + Redis 全量测试通过。
- 前端 oxlint、TypeScript 和 Vite 生产构建通过。
- OpenAPI 契约校验通过。
- 新版已部署，发布目录 `20260801033145`，主资源 `index-C-LOzcmw.js`，主样式 `index-DPAqjC5B.css`。
- 公网端到端回归 21 项全部通过：真实 zip 上传到 OSS、未发布项目 404、目录树含推导目录与嵌套目录、README 优先、Go 与 Markdown 语言识别、中文内容正确、文件名搜索只返回命中项、二进制 415、三种穿越路径均 400、不存在文件 404、代码包下载 307。
- 回归临时账号、项目及 Redis 限流键已清理；服务器临时目录和测试二进制已删除；清理后仅剩 2 个真实用户、1 个真实项目，公网目录为 4 个正常项目。

### 回归过程记录的两个脚本缺陷（非产品问题）

- 预签名上传必须原样回传响应中的签名请求头，写死请求头会导致 OSS 403，进而误报为 Gateway 读取失败。
- Go 的 `encoding/json` 默认将 `&` 转义为 `\u0026`，shell 取字面值后必须还原，否则预签名 URL 查询参数会被 OSS 视为单个畸形参数。前端使用 `JSON.parse` 不受影响。
- 回归期间还触发了注册限流（每小时 5 次/IP，返回 429），证明限流生效；已定向删除来源为 `127.0.0.1` 的测试计数键。

### 注意事项

- 归档缓存是单实例进程内存；多实例部署后每个实例会各自缓存，内存预算需按实例数估算。
- tar.gz 读单文件需重新顺序扫描流，CPU 成本高于 zip 的随机访问；大量 tar.gz 项目频繁浏览时应考虑预展开缓存。
- 当前代码高亮仅返回语言标识并以纯文本渲染，尚未接入 highlight.js 着色；后续可复用文档页已有的 highlight 能力。
- 代码浏览未提供单文件下载（需求 6.7 第 7 项），当前只能下载整个代码包。

### 下一步

1. 代码预览接入语法高亮着色，并支持单文件下载。
2. 通知点击跳转到对应项目文档或作者中心。
3. 为集成测试配置独立测试数据库，与生产库隔离。

## 2026-08-01：文档选区评论四个缺陷修复

### 背景

项目负责人反馈文档阅读页评论区四个问题：Emoji 面板被遮挡、关闭后无法重开、发布评论白屏、评论框未贴在选中内容下方。
已先在真实浏览器复现全部问题，确认根因后再修复，避免凭推测改代码。

### 白屏缺陷（最严重）

- 现象：点击“发布评论”后整页变空，浏览器控制台报 `Cannot read properties of null (reading 'map')`。
- 根因一（后端）：MySQL 评论仓库的 `Create` 等写操作不填 `Replies`，nil 切片序列化为 `"replies":null`，违反了契约中 replies 为数组的约定。内存仓库会主动置空数组，所以本地开发时不暴露。
- 根因二（前端）：`mapDocumentComment` 被写在 `setComments` 的 updater 回调**内部**调用，异常抛在 React 状态处理阶段，不在 `try/catch` 覆盖范围，因此直接卸载整颗组件树而不是提示失败。
- 修复：后端新增 `normalizeCommentReplies`，在全部 5 个单条评论响应写出点保证 replies 为数组；前端 `mapDocumentComment` 对 `replies` 与 `reply_count` 容错，并将映射移到 updater 外部，使异常能被 `try/catch` 捕获。

### 连带发现并修复：解决或编辑评论会清空回复

- `resolveComment` 和 `editComment` 用不携带回复的单条响应整体替换本地评论，导致该线程已有回复从界面消失。
- 修复：合并时保留本地 `replies` 和 `replyCount`。

### Emoji 面板两个缺陷

- 无法重开的根因：emoji-mart 的 `onClickOutside` 在任意文档点击时都会触发（内部判断仅为 `e.target != element`），与开关按钮的 toggle 互相干扰。
- 修复：移除对 `onClickOutside` 的依赖，改由 `EmojiPicker` 自行在捕获阶段监听 `pointerdown`，容器内点击不关闭，并支持 Esc 关闭。
- 被遮挡的根因：弹层固定向下开（`top: 34px`），而评论框位于视窗底部，面板落在可视区域之外。
- 修复：根据按钮下方剩余空间自动选择向上或向下弹出。

### 评论框位置

- 原先为 `position: sticky; bottom: 18px`，总是贴在屏幕底部。
- 修复：选区时记录选区底部相对文章容器的偏移，评论框改为绑定该偏移的绝对定位，并加上指向选区的小箭头；从侧边栏直接新建评论时仍回退到原有底部位置。

### 验证

- 新增 `TestCommentResponsesNeverReturnNullReplies`，使用模拟 MySQL 行为的仓库桩（写操作返回 nil Replies），覆盖创建、编辑、解决、回复和编辑回复 5 个响应。
- 已反向验证该测试有效：临时禁用修复后测试失败并报出 `"replies":null` 响应，恢复后通过。（注：最初用内存仓库写的版本无法复现缺陷，已改为仓库桩。）
- `gofmt`、`go vet`、本地全量测试、前端 oxlint、TypeScript 和 Vite 构建均通过。
- 真实浏览器验证（本地开发服务器代理到线上 API）：
  - 发布评论不再白屏，新评论正常进入列表，控制台无 `Cannot read properties of null` 错误。
  - Emoji 面板完整可见且 `em-emoji-picker` 已渲染。
  - Emoji 按“开-关-开-关-点空白关-再开”六步反复验证均正确。
  - 选择表情能正确插入输入框。
  - 评论框定位数值证据：`computedPosition` 从 `sticky` 变为 `absolute`，`inlineTop=301.195px`，且 `composerTop(463) - articleTop(162) = 301` 与之吻合。
- 新版已部署，发布目录 `20260801040834`，主资源 `index-D4GgQG1p.js`，主样式 `index-Ca2rRfVI.css`；Gateway 与 Nginx 均 `active`，内网 `/healthz`、`/readyz` 和公网 `/api/v1` 均 200。
- 验证期间在生产库产生的 2 个临时账号和 2 条评论已定向删除，用户回到 2 个、评论回到 4 条；Redis 限流键已清理。

### 注意事项

- 本次为复现问题使用了本地 Vite 开发服务器代理到线上 API（`VITE_API_PROXY_TARGET` 写在 gitignore 的 `.env.local`，验证后已删除）。这种方式会向生产库写入测试数据，必须验证后立即清理。
- 单条评论响应（创建/编辑/解决/回复）本质上不携带完整回复树，前端合并时必须保留本地回复，不能直接整体替换。
- 以后在 `setState` 的 updater 回调内不要做可能抛异常的数据映射，否则错误会绕过 `try/catch` 并造成白屏。

### 下一步

1. 代码预览接入语法高亮着色，并支持单文件下载。
2. 为关键页面增加 React 错误边界，避免单一渲染异常卸载整站。
3. 为集成测试配置独立测试数据库，与生产库隔离。

## 2026-08-01：阅读页真实数据与协作编辑器布局

### 阅读页展示示例假数据

- 现象：作者发布的真实项目打开后正文是硬编码的 Atlas 示例内容。
- 根因一：文档仓库只有 4 个种子项目的硬编码数据，已发布项目（正文存于 `managed_projects.description`）没有任何文档，API 返回 404。
- 根因二：前端把文档 slug 硬编码为 `quick-start`，已发布项目的 slug 是 `overview`，必然取不到，随后回退到静态假内容。
- 修复：新增 `managedDocumentRepository` 将已发布项目正文作为真实文档提供，未命中再回退种子项目；新增 Markdown 解析提取标题、大纲和稳定内容块（块 ID 用内容哈希，未改动的块跨部署保持稳定）；文档仓库接口加入 context 与 error；前端改为先取目录再按首篇 slug 加载。
- 同步移除阅读页静态假正文、侧栏假节点和两条演示评论，改为真实空状态。假内容会让作者误以为文档已发布，因此不保留任何示例兜底。

### 协作编辑器布局完全错乱

- 根因：Crepe 主题样式只在 `RichMarkdownEditor` 中引入，而协作编辑器是独立的惰加载 chunk，直接进协作编辑时没有加载任何编辑器样式。构建产物可证：此前只有 `RichMarkdownEditor` chunk 附带 79 KB 样式表。
- 修复：协作编辑器补充引入 Crepe common 与 frame 主题，构建后两个编辑器共享同一份样式 chunk，协作 chunk 的依赖清单已包含该样式表。
- 同时按在线文档工具的阅读排版调整：限宽 760px 居中、字号 15px、行高 1.8。

### 阅读体验升级（对齐在线文档工具）

- 新增 `DocumentReader.tsx`（组件）与 `documentReaderUtils.ts`（纯函数与 Hook）。
- Mermaid 图表在阅读页直接渲染，此前仅编辑器和项目概览支持，阅读页显示为源码；渲染失败时保留原始定义。
- 代码块增加语言标识和一键复制；行内代码与代码块按语言和换行区分。
- 标题悬停浮现锚点按钮，可复制到小节的链接。
- 图片支持惰加载、`oss://` 地址转换和点击放大（Esc 或点击遮罩关闭）。
- Bilibili 链接自动转为内嵌播放器，iframe 限制 sandbox 与 referrer。
- 新增文档内搜索：命中黄色高亮、当前项强调、显示计数、回车与 Shift+回车前后跳转。实现使用 CSS Custom Highlight API，**不修改 DOM 结构**，因此不会破坏选区评论依赖的 `data-block-id` 锚点。
- 补充 GFM 任务列表样式和表格小屏横向滚动。
- 合并了 App.tsx 中重复的 `MermaidDiagram` 定义。

### 验证

- Go 全量测试通过；新增文档仓库测试覆盖已发布项目正文、大纲与稳定块提取、未知 slug、种子回退和无标题降级。
- 前端 oxlint、TypeScript 和 Vite 构建通过。
- 线上接口验证：`/api/v1/projects/my-project/documents` 返回真实文档节点，`/documents/overview` 返回作者真实 Markdown、两项大纲和稳定块。
- 真实浏览器验证：阅读页展示真实正文且 `hasAtlasFakeContent=false`；Mermaid 渲染出 SVG 且不再显示源码；标题锚点按钮存在且默认隐藏；文内搜索命中计数 `1 / 1` 且高亮已注册；选区评论回归正常。
- 协作编辑器浏览器验证：工具条正常、字号 15px、行高 27px、正文限宽 760px、引用块左边框 3px、Go 代码块有语法高亮，输入与自动保存正常（修订号递增）。
- 发布目录：`20260801043533`（真实文档）、`20260801043902`（协作布局）、`20260801050814`（阅读体验）。

### 注意事项

- 线上 8443 是自签名证书，浏览器自动化无法跳过警告页，因此前端验证统一采用本地开发服务器代理到线上 API（代码与线上一致），验证后删除 `.env.local`。
- 协作编辑入口只对 `can_edit` 显示，管理员并非所有项目的所有者，因此验证时需用自己的项目；本次为此创建的临时项目已删除，已确认无孤儿快照。
- 块 ID 采用内容哈希：未改动的块保持稳定，但该块被编辑后 ID 会变化、对应评论失去锚点。要做到编辑后仍保留锚点，需为每个块持久化独立 ID。
- 当前一个项目只有一篇正文（存于 `description`）。多层文档树需新建 documents 表和整套文档 CRUD，属于架构级改造。

### 下一步

1. 代码预览接入语法高亮着色，并支持单文件下载。
2. 为关键页面增加 React 错误边界。
3. 为集成测试配置独立测试数据库。
4. 多层文档树（知识库结构），支持一个项目多篇文档。

## 2026-08-01：知识库结构（多层文档树）

### 背景

此前一个项目只能有一篇正文（存在 `managed_projects.description` 字段），无法组织多篇文档，是与在线文档工具差距最大的一块。

### 后端

- 新增 `project_documents` 表与迁移 `000008`，含项目内 slug 唯一约束、目录索引、自引用父级外键和软删除。
- 新增文档仓库（内存 + MySQL 双实现）：列表、按 slug 查找、创建、更新、改写正文、移动和删除。
- 目录树按父子关系组装，同级按 sort_order 再按标题排序，递归限制 5 层。
- 父级校验阻断三类非法结构：指向自身、指向自己的子孙（成环）、超过层级上限。
- 删除父文档时级联移除全部后代，内存与 MySQL 行为一致。
- 新增作者端 API：文档列表/新建/更新/移动/删除，权限为项目所有者或 editor 协作者，无权限返回 404 而非 403。
- 项目仓库补充 `FindByID`，供作者端编辑草稿与已发布项目。
- 阅读侧改为读取文档表；项目尚未建文档时仍把项目正文当单篇文档，旧项目升级后不会出现空白文档页。
- OpenAPI 更新为 48 条路径、60 个 schema。

### 前端

- 新增 `ProjectDocumentTree.tsx`：作者中心侧栏的文档目录面板。
- 支持新建根文档与子文档、双击重命名、上下移调整同级顺序、→ 降级 / ← 升级调整层级、折叠展开、删除。
- 选中文档后编辑区切到该文档正文，顶部显示当前文档横幅与“回到项目正文”入口；文档正文独立自动保存，与项目草稿保存互不影响。
- 新建与删除均使用**页面内联交互**，不用浏览器原生 `prompt`/`confirm`：原生弹窗观感粗糙，且在部分浏览器上下文会被直接屏蔽。删除采用内联红色确认条并提示连带删除的子文档数量。

### 浏览器验证中发现并修复的两个前端缺陷

- 保存状态不显示“文档已保存”：保存成功后 `setActiveDocument` 更新了 markdown，触发了载入草稿的 effect，把 `saved` 状态重置回 `idle`。改为用 ref 比对文档 id，只在真正切换文档时重载草稿。
- 切走再切回内容丢失：文档树缓存的是加载时的旧 markdown，编辑器保存后树数据未刷新，重新点击时把旧内容传回编辑器。新增 `refreshToken`，保存成功后通知文档树重拉数据。

### 验证

- Go 全量测试通过；新增 5 组文档树测试覆盖增删改查、移动、环检测、级联删除、权限边界与旧项目兼容。
- 应用服务器真实 MySQL 全量测试通过；迁移已应用到生产库，索引与三个外键已校验。
- 前端 oxlint、TypeScript 和 Vite 构建零警告通过。
- 公网端到端回归 14 项全部通过：无文档时回退项目正文、多层创建、目录树嵌套、重复 slug 409、自引用 422、移动置顶、阅读侧真实正文与大纲、匿名 401、级联删除。
- 浏览器验证：内联新建根/子文档、层级缩进（paddingLeft 8px → 24px）、点击切换编辑区、层级调整、内联删除确认条（“删除《使用指南》及其 1 篇子文档？”）、双击重命名均正常。
- 保存持久化已取得最强证据：**完全刷新页面后重新打开文档，内容仍在**，且无失败的 PUT 请求。
- 验证临时项目及其 6 篇文档已随外键级联清理，仅保留真实项目。

### 注意事项

- 中文标题无法转写为 ASCII slug，当前回退为 `doc-<时间戳>`。对中文内容站点而言这意味着文档地址普遍不可读，后续应开放 slug 手动编辑。
- 协作编辑仍绑定在项目级，尚未下沉到文档级；多篇文档同时协作会互相串内容，属于必预修项。
- 文档软删除后 slug 仍占用唯一约束（约束不含 deleted_at），重用同名 slug 会 409；如需重用需调整约束或改为硬删除。

### 下一步

1. 协作编辑从项目级下沉到文档级（必预）。
2. 文档 slug 开放手动编辑。
3. 代码预览接入语法高亮着色，并支持单文件下载。
4. 为关键页面增加 React 错误边界。
5. 为集成测试配置独立测试数据库。
